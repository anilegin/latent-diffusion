project:
  root: .

data:
  # Path to local COCO 2017 dataset root.
  coco_root: /path/to/coco2017

outputs:
  root: outputs
  vae_dir: outputs/vae
  ldm_dir: outputs/ldm
  latent_dir: outputs/latents
  sample_dir: outputs/samples
  log_dir: logs

cache:
  root: cache
